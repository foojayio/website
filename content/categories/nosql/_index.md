---
# A TERM CONTENT FILE EXISTS ONLY TO FIX THE NAME. Hugo titles a term by
# title-casing it, which turns "nosql" into "Nosql"; this file overrides that
# with the way the technology writes itself. partials/category-name.html then
# feeds the same title to every chip and pill, so the card and the page it
# links to finally agree.
#
# The folder name is the slug, so the URL (/today/category/nosql/) is unchanged
# -- posts keep `categories: - nosql` and nothing has to be re-tagged.
title: "NoSQL"
---
